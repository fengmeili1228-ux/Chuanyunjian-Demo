# Docker Compose 环境下使用的 profile
# 覆盖本地 localhost，使用服务名 mysql / redis 进行容器间通信
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/chuanyunjian?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root8.0
  data:
    redis:
      host: redis
      port: 6379
      database: 0
